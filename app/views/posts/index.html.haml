- @posts.each do |post|
	%h2= link_to post.title, post
	%p
		= post.comments.count
		Comments
	%p
		= post.get_upvotes.size
		Likes
	= link_to (image_tag post.image.url(:small)), post


= link_to "Add Inspiration", new_post_path